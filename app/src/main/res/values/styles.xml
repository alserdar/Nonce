<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/ourBlue</item>
        <item name="colorPrimaryDark">@color/ourBlue</item>
        <item name="colorAccent">@color/ourBlue</item>
        <item name="android:textColor">@color/darkness</item>
        <item name="android:textColorHint">@color/darkness</item>

    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
